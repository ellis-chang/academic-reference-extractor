# NOTE: This file lists the dependencies that was used to complete the project at the time.
# It is recommended to create a virtual environment and install these exact versions to ensure compatibility.
# You can install the dependencies using:
# pip install -r requirements.txt

# Core PDF processing libraries
pdfplumber~=0.11.7
pypdf>=3.0.0

# Data handling
pandas~=2.3.2
openpyxl~=3.1.5

# Environment variables
python-dotenv>=1.2.1
requests>=2.28.0
beautifulsoup4>=4.12.0
anthropic>=0.18.0
python-dotenv>=1.2.1
